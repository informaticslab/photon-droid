<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="gov.cdc.mmwrexpress" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!--
  Creates a custom permission so only this app can receive its messages.

  NOTE: the permission *must* be called PACKAGE.permission.C2D_MESSAGE,
        where PACKAGE is the application's package name.
    -->

    <application
        android:name=".AppManager"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher_debug"
        android:label="@string/app_name_dev"
        tools:replace="icon,label"
        android:theme="@style/AppTheme" >

        <!-- Pushwoosh -->
        <meta-data android:name="com.pushwoosh.appid" android:value="BDF38-637C3" tools:replace="name,value" />
        <meta-data android:name="com.pushwoosh.senderid" android:value="@string/fcm_sender_id" tools:replace="name,value" />

        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name_dev"
            tools:replace="label"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
